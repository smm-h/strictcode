[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "strictcode"
version = "0.0.1"
description = "Name placeholder for strictcode. A linter for architecture, with tiered auto-fixes."
readme = "README.md"
